'use client'
import React, {FC} from 'react';
import {SearchFieldsList} from "@/components/SearchFieldsList";



const Search: FC = () => {
    return (
            <SearchFieldsList/>
    );
};

export default Search;